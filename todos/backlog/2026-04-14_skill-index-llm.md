# LLM Agent — Curated List & Extended Index

Build `agents/llm-dev.md` curated section and wire extended index classification
for LLM / agent framework skills.

## Scope

- Audit the local skill inventory for LLM / LangGraph / prompt engineering / agent
  framework skills
- Evaluate Anthropic-published skills (e.g. `claude-api` is bundled with
  Claude Code) as candidates for curated
- Decide classification boundary between `llm-dev` and `python-dev` (overlap
  on LangGraph, FastAPI serving LLM apps)

## Current state

`agents/llm-dev.md` has no curated section. `claude-api` (Claude Code built-in)
is a natural candidate but not yet referenced.

## Open questions

- Include `claude-api` (built-in) in curated? It's always available, so yes
  probably
- Where does `vercel:ai-sdk` live — `llm-dev` or `nextjs-dev`?

## Out of scope

- Other platforms

## Status

Backlog.
